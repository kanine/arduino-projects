# Journal — 2026-03-29

> Summarises commits from 2026-03-29 13:28:19 AEDT to 2026-03-29 14:35:25 AEDT / 7 commits

## ESP32 Time-of-Flight Firmware And Documentation (d689ee5, 727ca5a)

- Documented the ESP32 Time-of-Flight webhook firmware in a dedicated PRD so implementation decisions stay aligned with product intent.
- Ported the `timeofflightwebhook2` sketch to a standalone Pololu runtime flow so sensing, payload generation, and transport behavior are managed directly in firmware.
- Added supporting runtime modules (`config.h`, `http_core.h`, `ota_core.h`) to separate configuration, network behavior, and OTA handling for easier iteration.
- Added wiring and sample response docs alongside the sketch to reduce ambiguity during bring-up, testing, and integration checks.
- Added detailed VL53L1X reference notes to support parameter tuning and sensor behavior decisions during firmware development.

## Agent Workflow Skill Consolidation (d1edef0, 1aede16)

- Added reusable agent skill files for push and journal workflows to standardize repeatable git operations and reduce manual process drift.
- Added a VL53L1X reference skill document in the agent area so sensor facts are available consistently during future coding sessions.
- Reorganized DFR0032 buzzer documentation into the `docs/sound` section to improve information architecture and make topic-based lookup easier.
- Refined `pushit` instructions to tighten commit/push behavior and removed an unnecessary co-author requirement to better match current workflow expectations.

## ESP32 Webhook Feedback And Hardware Verification (508b813, 1eb1a0f, 0241903)

- Fixed the ESP32 HTTP POST callsites to match the current core's `HTTPClient` API so the webhook sketch compiles cleanly again on the installed toolchain.
- Added a webhook test mode to validate only `HTTP 2xx` plus `"success": true`, making it easier to isolate transport and response handling during bring-up.
- Added live request-log watching and serial-oriented diagnostics support so webhook traffic and firmware behavior can be checked more quickly while iterating.
- Split status LED and speaker control so the LED remains on GPIO2 while the speaker moves to D4, allowing visual success feedback without forcing audible success chirps.
- Updated the project wiring documentation to reflect the new speaker pin and clarified that the speaker is now reserved for failure alerts and explicit hardware verification.
- Removed the temporary Adafruit VL53L1X reference skill after it was no longer needed in the workflow.
